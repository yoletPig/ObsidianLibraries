### 阶段一：VLM 基础与架构深度拆解（4 - 6 周）

**阶段核心目标**：彻底打通“图片如何转换为 Token 喂给 LLM”的技术链路，手绘主流 VLM 架构图，并在代码层面理解特征流转，最终完成一次 VLM 的 LoRA 微调实战。

---

### 📅 逐周学习路线与 MVP 交付

#### 第 1 周：视觉编码器（Vision Encoder）与表征基础

* **学习重点**：
* **ViT (Vision Transformer)**：理解 Patching 机制，图片 $H \times W \times C$ 如何拆解为序列 $N = (H/P) \times (W/P)$ 并添加 Position Embedding。
* **CLIP (Vision Tower)**：双塔架构（Text + Image Encoder），理解对比学习（Contrastive Learning）损失函数与特征对齐原理。
* **SigLIP**：理解其为何优于 CLIP（使用 Sigmoid Loss 替代 Softmax，支持更大 Batch 且对齐质量更高）。

* **推荐论文**：
* ViT 奠基论文: *An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale*
* CLIP: *Learning Transferable Visual Models From Natural Language Supervision*
* SigLIP: *Sigmoid Loss for Language Image Pre-Training*

* **💡 第 1 周 MVP 交付**：
> 编写 Python 脚本，调用 Hugging Face `transformers` 加载 CLIP/SigLIP 的 Vision Tower，输入一张图片，提取其 Output Feature Tensor，打印并记录其 Shape（例如 `[1, 576, 1152]`），说明 `576` 和 `1152` 的物理含义。

#### 第 2 周：模态对齐桥梁（Connectors / Projector）

* **学习重点**：
* **Linear / MLP Projector (LLaVA 路线)**：简单高效的 2 层 MLP，如何将视觉维度的 Feature（如 1152）投影映射到文本 LLM 的 Hidden State 维度（如 4096）。
* **Q-Former (BLIP-2 路线)**：使用可学习的 Learned Queries 通过 Cross-Attention 抽取固定数量的视觉 Token。
* **Perceiver Resampler / Abstractor**：下采样与信息压缩机制。
* **对比分析**：不同 Projector 在计算开销、Token 压缩率以及细节保留能力上的 Trade-off。
* **推荐论文**：
* LLaVA: *Visual Instruction Tuning* (NeurIPS 2023)
* BLIP-2: *BLIP-2: Bootstrapping Language-Image Pre-training with Frozen Image Encoders and Large Language Models*

* **💡 第 2 周 MVP 交付**：
> 用 PyTorch **手写一个独立的 `MLP Projector` 模块**（包含 2 层 Linear + GELU 激活），输入 shape 为 `[Batch, 576, 1024]` 的模拟视觉 Tensor，输出为 `[Batch, 576, 4096]` 的 LLM 适配 Tensor。

#### 第 3 周：主流 VLM 架构与图像 Token 处理全流程

* **学习重点**：
* **LLaVA-1.5 架构**：`<image>` 占位符替换逻辑、`input_ids`（文本）与 `pixel_values`（视觉）在 Embedding 层的拼接。
* **动态分辨率与任意长宽比 (Qwen2-VL)**：
* 理解 Naive-Dynamic-Resolution（动态分辨率）与 2D-RoPE 空间位置编码。
* 理解 Patch Merging（将 4 个相邻 Patch Token 合并为 1 个，降低 Token 数量）。
* **多图与视频扩展**：多图 Token 拼接策略，视频在时间轴（Temporal）上的 Token 抽样。
* **推荐论文**：
* LLaVA-v1.5: *Improved Baselines with Visual Instruction Tuning*
* Qwen2-VL: *Qwen2-VL: To See the World More Clearly*

* **💡 第 3 周 MVP 交付**：
> 手画一份 **LLaVA-1.5 与 Qwen2-VL 的前向传播流程对比图**，标注一张图片在经过 Vision Encoder $\rightarrow$ Projector $\rightarrow$ Token Concat $\rightarrow$ LLM Backbone 过程中每一个节点的 Tensor 维度变化。

#### 第 4 - 5 周：VLM 代码拆解与 SFT / LoRA 微调实战

* **学习重点**：
* **源码研读**：阅读 Hugging Face `transformers` 库中 `LlavaForConditionalGeneration` 或 `Qwen2VLForConditionalGeneration` 的 `forward()` 函数。
* **多模态 Prompt 格式**：掌握 ChatML / LLaVA 格式中 `<|im_start|>user\n<image>\n...<|im_end|>` 的构造逻辑。
* **微调机制**：了解冻结 Vision Encoder、只训练 Projector + LLM LoRA 权重的训练策略。

* **实战工具**：`LLaMA-Factory` 或 `ms-swift`。
* **💡 第 4-5 周 MVP 交付**：
> 1. 准备一份 200~500 条样本的格式化多模态数据集（例如：图片 + 描述 / VQA 问答）。
> 2. 使用 `LLaMA-Factory` 完成一次 **Qwen2-VL-2B/7B** 或 **LLaVA-1.5-7B** 的 LoRA/QLoRA 微调。
> 3. 记录 WandB 训练 Loss 曲线，并导出微调后的 Checkpoint 进行推理测试（对比微调前后的回答差异）。
#### 第 6 周：阶段复盘与自测验收

* **学习重点**：
* 整理与固化前面几周的技术文档与代码库。
* 分析训练中的常见问题（如 GPU OOM、Vision Tower 显存占用、Loss 不收敛等）。
* **💡 第 6 周 终极自测清单（满足即可进入 Stage 2）**：

| 评估维度 | 自测问题 / 任务 | 达成标准 |
| --- | --- | --- |
| **理论计算** | 当一张 $336 \times 336$ 的图片输入 Patch Size 为 $14 \times 14$ 的 CLIP 时，进入 LLM 的视觉 Token 是多少个？ | 能推导出 $(336/14) \times (336/14) = 576$ 个 Token。 |
| **架构理解** | 为什么当前多数 VLM 训练时会冻结 Vision Encoder？ | 能准确说出参数量、计算开销、通用表征破坏风险与训练效率的关系。 |
| **代码实操** | 能否用 20 行以内的代码展示 `input_ids` 中的 `<image>` 标记是如何被 Projector 输出的 Visual Embeddings 替换的？ | 能在 PyTorch 中使用 `torch.where` 或索引拼接完成 Tensor 替换。 |
| **实战交付** | 是否在本地或云端成功运行过一次 VLM 的 LoRA 训练并产出可推理的 Checkpoint？ | 提供 WandB 训练 Log 图与实际推理对比例子。 |

---

### 🛠️ 建议使用的工具与代码库

1. **研读代码仓库**：
* `huggingface/transformers` (重点看 `src/transformers/models/llava` 和 `qwen2_vl`)
* `haotian-liu/LLaVA` (官方经典实现)
2. **微调框架**：
* `hiyouga/LLaMA-Factory` (推荐上手)
3. **实验跟踪**：
* `Weights & Biases (WandB)`
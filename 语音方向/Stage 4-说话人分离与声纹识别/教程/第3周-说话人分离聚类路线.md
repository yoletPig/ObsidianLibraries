# 第 3 周教程：说话人分离——聚类路线（主流工业方案）

> **本周要回答的三个问题**
> 1. 主流工业分离流水线的五个步骤是什么？每步的输入输出是什么？
> 2. 谱聚类为什么比 K-Means 更适合声纹聚类？亲和矩阵是什么？
> 3. 说话人数量怎么估？重叠语音为什么是聚类路线的先天缺陷？

对应学习计划：第 3 周。交付物：跑通 `pyannote.audio` 3.x 分离流水线，输出 RTTM 文件，用 `pyannote.metrics` 计算 DER 并拆解为 Miss + False Alarm + Confusion 三部分。

---

## 1. 第一性原理：分离 = "无监督的说话人分组"

### 1.1 问题定义

给定一段多人音频，输出**每个时刻是谁在说话**：

$$
\{(\text{start}, \text{end}, \text{speaker})\} \text{ 区间的集合}
$$

关键难点：**没有标签**（不知道有几个人、谁是谁），也不知道说话人数量。这是纯无监督问题。

### 1.2 聚类路线的核心假设

> **同一说话人的声纹向量彼此接近，不同说话人的彼此远离。**

于是分离 = **把切片的声纹向量聚类**，每个簇对应一个说话人。这就是主流工业方案（pyannote、NeMo MSDD 的基础）。

---

## 2. 五步流水线（工业标准）

$$
\text{音频} \xrightarrow{\text{① VAD/切块}} \xrightarrow{\text{② 声纹提取}} \xrightarrow{\text{③ 聚类}} \xrightarrow{\text{④ 重分段}} \text{RTTM}
$$

### ① 切块（Segmentation）

- 用 VAD 去掉静音，把语音切成片段；
- 常用**滑动窗口**：窗长 1.5 s、帧移 0.75 s（50% 重叠）；
- 输出若干候选片段，每段假设"只含一个说话人"。

**为什么 1.5 s**：太短声纹不稳（第 1 周讲过），太长容易横跨两人。1.5 s 是经验甜点。

### ② 声纹提取

对每个片段用 ECAPA 提取声纹向量。1 小时音频、0.75 s 帧移 → 约 4800 段 → 4800 个向量。

### ③ 聚类（核心）

把 4800 个向量聚成 $K$ 簇，$K$ = 说话人数。难点：**$K$ 未知**。

### ④ 重分段（Re-segmentation）

聚类只给出每段的说话人，但段边界粗糙。重分段用更细粒度（逐帧）的声纹或 VAD 边界，把标签平滑成连续区间，消除碎片。

### ⑤ 输出 RTTM

RTTM 是分离的标准格式：

```
SPEAKER file 1 12.34 5.67 <NA> <NA> speaker_0 <NA> <NA>
```

含义：`文件名 起始 12.34s 时长 5.67s 说话人 speaker_0`。

---

## 3. 聚类算法：谱聚类为什么胜出

### 3.1 K-Means 的局限

K-Means 假设簇是**凸形、各向同性**的，用欧氏距离找球心。但声纹簇在超球面上形状不规则，且 K-Means 需要预设 $K$、对初始化敏感。

### 3.2 谱聚类（Spectral Clustering）

谱聚类基于**图论**：

1. **构造亲和矩阵** $\mathbf{A}$：$A_{ij} = \text{sim}(\mathbf{e}_i, \mathbf{e}_j)$（声纹余弦相似度，阈值化或 softmax 化）；
2. **构建图拉普拉斯** $\mathbf{L} = \mathbf{D} - \mathbf{A}$；
3. 取 $\mathbf{L}$ 的**最小 $K$ 个特征向量**，把每行当新坐标 → 降维后的点"簇内紧、簇间散"；
4. 在新空间做 K-Means。

**为什么更适合声纹**：谱聚类能捕捉**非凸簇结构**（通过图的连通性），且亲和矩阵直接编码"两两相似度"，与声纹的度量天然匹配。这是工业界普遍选它的原因。

### 3.3 说话人数量估计

谱聚类需要 $K$。常用手段：

- **特征值法**：拉普拉斯矩阵的特征值会呈现"前 $K$ 个很小、之后跳变"的模式，跳变点即 $K$；
- **NME-SC（pyannote 3.x 用的思路）**：多尺度嵌入 + 自动估数；
- **预设 + 校验**：先猜一个范围，用轮廓系数等选最优。

**估错 $K$ 的后果**：估多 → 同一人被拆成多个；估少 → 不同人被合并。这是聚类路线的主要误差源之一。

---

## 4. 重叠语音：聚类路线的先天缺陷

### 4.1 问题本质

聚类假设"每段只含一个说话人"，且**每个时刻只能分配一个标签**。当两人同时说话（overlap），该段的声纹是两人混合，聚类只能选一个 → **另一个人的语音被错标或漏标**。

### 4.2 缓解手段

- **OSD（Overlap Speech Detection）**：先检测重叠区，重叠区允许两个标签；
- 但 OSD 只能标记"这里重叠"，无法精确拆出每人说了什么——**这是聚类路线的根本上限**。

彻底解决重叠要端到端方法（第 4 周）。

---

## 5. 实现与验证（交付核心）

### 5.1 跑通 pyannote.audio 3.x

```bash
pip install pyannote.audio
# 需要 HF token 并接受模型条款
```

```python
from pyannote.audio import Pipeline
import torch

pipeline = Pipeline.from_pretrained(
    "pyannote/speaker-diarization-3.1",
    use_auth_token="YOUR_HF_TOKEN")
pipeline.to(torch.device("cpu"))   # 无 GPU 用 CPU

# 双人对话录音
diarization = pipeline("meeting_2spk.wav")

# 输出 RTTM
with open("output.rttm", "w") as f:
    diarization.write_rttm(f)

# 打印说话人区间
for turn, _, speaker in diarization.itertracks(yield_label=True):
    print(f"[{turn.start:7.2f} - {turn.end:7.2f}] {speaker}")
```

**预期**：打印出带说话人标签的时间区间，两人交替出现。

### 5.2 计算 DER 并拆解

```python
from pyannote.metrics.diarization import DiarizationErrorRate
from pyannote.core import Annotation

metric = DiarizationErrorRate()

# 参考标注（人工标注的 RTTM，需自己准备 reference.rttm）
from pyannote.database.util import load_rttm
reference = load_rttm("reference.rttm")["meeting_2spk"]

der = metric(reference, diarization, detailed=True)
print(f"DER = {der['diarization error rate']*100:.2f}%")
print(f"  Miss        = {der['missed detection']*100:.2f}%")
print(f"  False Alarm = {der['false alarm']*100:.2f}%")
print(f"  Confusion   = {der['confusion']*100:.2f}%")
```

**DER 三分解**（本周自测题，必须记住）：

$$
\text{DER} = \underbrace{\text{Miss}}_{\text{漏检语音}} + \underbrace{\text{False Alarm}}_{\text{把噪声当语音}} + \underbrace{\text{Confusion}}_{\text{说话人标错}}
$$

- **Miss**：真实有语音但系统没标（VAD 漏检）；
- **False Alarm**：无语音（静音/噪声）但系统标了人；
- **Confusion**：检测到了语音，但说话人标错了（聚类错误）。

**分析价值**：Miss/FA 高 → 改 VAD/切块；Confusion 高 → 改声纹模型或聚类。这是分离调优的诊断地图。

---

## 6. 工程权衡与失效模式

### 6.1 权衡

- **窗长**：短 → 时间分辨率高但声纹不稳；长 → 反之。1.5 s 是甜点；
- **说话人数估计**：估多伤 Confusion，估少伤完整性；
- **重叠处理**：纯聚类不处理重叠，需加 OSD，但能力有限。

### 6.2 失效模式

1. **说话人数估错**：同一人被拆或不同人被合并。诊断：Confusion 高；修复：改用自动估数（NME-SC）或提供人数先验。
2. **短发言漏标**：说话人只说了一两个字，声纹不稳被归入别人或丢弃。修复：降低切块长度阈值 + 后处理合并。
3. **重叠区错标**：两人同时说话，聚类只给一个标签。诊断：听重叠区音频；修复：OSD + 端到端（第 4 周）。
4. **信道不一致**：不同说话人录音条件差异大，声纹分布被信道污染。修复：前端降噪（Stage 3）+ 域适应。

---

## 7. 延伸思考题（含解析）

**Q1**：1 小时会议、1.5 s 窗、0.75 s 帧移，产生多少段待聚类向量？谱聚类复杂度量级？
**A**：$(3600 - 1.5)/0.75 \approx 4800$ 段。谱聚类需构造 $4800 \times 4800$ 亲和矩阵并做特征分解，复杂度约 $O(n^2)$ 到 $O(n^3)$——长音频是瓶颈，需分块或近似方法。

**Q2**：谱聚类相比 K-Means 的优势是什么？
**A**：谱聚类基于图的连通性，能捕捉非凸簇结构，且亲和矩阵直接编码声纹两两相似度；K-Means 假设凸形球簇、对初始化敏感、需预设 $K$。声纹簇形状不规则，谱聚类更契合。

**Q3**：DER 的三个组成部分分别指向什么模块的问题？
**A**：Miss 与 False Alarm 指向 VAD/切块（检测层）；Confusion 指向声纹模型与聚类（分组层）。诊断时先看哪部分高，再针对性调优。

**Q4**：为什么聚类路线处理不了重叠语音？
**A**：聚类的输出空间是"每个片段一个互斥标签"，重叠片段只能选一个说话人。这是输出空间设计决定的，不是调参能解决的——必须换成能输出多标签的端到端方法。

**Q5**：说话人数量估计错会怎样？怎么缓解？
**A**：估多 → 同人被拆（Confusion 升）；估少 → 异人被合（漏说话人）。缓解：用特征值跳变法 / NME-SC 自动估数，或业务侧提供人数先验。

---

## 本周交付清单

- [ ] 跑通 pyannote 3.x，对双人录音输出 RTTM 与说话人区间。
- [ ] 用 `pyannote.metrics` 算 DER，拆解 Miss/FA/Confusion 三部分。
- [ ] 分析误差主要来源（检测层还是分组层）。
- [ ] 能闭卷回答：五步流水线、谱聚类优势、重叠缺陷根因。

# 第 1 周教程：TTS 全景与声码器演进

> **本周要回答的三个问题**
> 1. 从文本到波形要经过哪些环节？"声学模型"和"声码器"各管什么？
> 2. 梅尔谱 → 波形的"一对多"难题是什么？VAE / Flow / GAN 各自怎么破解？
> 3. HiFi-GAN 为什么能做到并行生成且高保真？它的判别器在挑什么毛病？

对应学习计划：第 1 周。交付物：画出「Tacotron2 + HiFi-GAN」与「VITS 端到端」两条推理链路对比图；用官方模型合成你的自我介绍，做主观 MOS 盲评。

---

## 1. 第一性原理：TTS 的两段式分解

### 1.1 任务定义

给定文本，生成自然、流畅、（可选）像特定人的语音波形。难点在于：**文本是离散、稀疏、无韵律信息的符号序列，而语音是连续、稠密、带丰富韵律的信号**——两者之间隔着巨大的信息鸿沟。

### 1.2 经典两段式分解

$$
\text{文本} \xrightarrow{\text{文本前端}} \text{音素序列} \xrightarrow{\text{声学模型}} \text{梅尔谱} \xrightarrow{\text{声码器}} \text{波形}
$$

- **文本前端（Text Frontend）**：文本 → 音素（G2P, grapheme-to-phoneme），处理多音字、数字、标点停顿；
- **声学模型（Acoustic Model）**：音素 → 梅尔谱，决定"说什么、什么韵律"；
- **声码器（Vocoder）**：梅尔谱 → 波形，决定"音质"。

这个分解的意义：**把难题拆开**。声学模型专注内容与韵律，声码器专注音质，各自可独立优化。

---

## 2. 声学模型演进：Tacotron → FastSpeech

### 2.1 Tacotron（Seq2Seq + Attention）

把 TTS 当序列到序列：编码器读音素，解码器**自回归**逐帧生成梅尔谱，用 attention 对齐"当前音素该生成哪几帧"。

**问题**：
- 自回归慢（逐帧生成）；
- attention 对齐易出错 → 跳字、重复、漏字；
- 鲁棒性依赖 attention 的单调性约束。

### 2.2 FastSpeech（非自回归 + 显式时长）

FastSpeech 的突破：**不让模型自己学对齐，而是显式预测每个音素的时长（duration）**。

$$
\text{音素序列} \xrightarrow{\text{时长预测器}} \text{每个音素的帧数} \xrightarrow{\text{长度调节器展开}} \text{帧级序列} \xrightarrow{\text{解码}} \text{梅尔谱}
$$

**关键**：有了显式时长，就能**并行**生成所有帧（不用自回归），速度提升数十倍；对齐由时长决定，不再依赖脆弱的 attention。

**时长标签哪来**：用预训练的 teacher（如 Tacotron2）的 attention 对齐提取。这是"知识蒸馏"思想在 TTS 的早期应用。

### 2.3 FastSpeech 2 的改进

加入**音高（pitch）预测器**和**能量（energy）预测器**，把韵律也显式建模，可控性更强。FastSpeech 2 是工业 TTS 的基石架构。

---

## 3. 声码器：梅尔谱 → 波形的"一对多"难题

### 3.1 难题本质

梅尔谱只保留**幅度**，丢失了**相位**。而重建波形需要两者。更麻烦的是：

$$
\text{同一个梅尔谱} \;\longleftrightarrow\; \text{无数种可能的波形}
$$

因为相位不确定，且人耳对相位不敏感。这就是"一对多"问题——声码器的任务是**从一对多的映射里，挑出一个听起来自然的波形**。

### 3.2 三代声码器

**第一代：Griffin-Lim（无模型）**

迭代地从幅度谱估计相位（你在 Stage 1 第 2 周实现过）。快但音质差、有"金属感"伪影——因为它无法建模真实语音的相位结构。

**第二代：WaveNet（自回归）**

DeepMind 的 WaveNet 用**因果卷积逐采样点**生成波形，每个采样点以前面所有点为条件：

$$
p(x) = \prod_{t} p(x_t \mid x_{<t})
$$

音质极好（首次达到自然水平），但**逐采样点自回归**——16 kHz 意味着每秒要生成 16000 个点，每点一次前向，**慢到无法实用**。

**第三代：HiFi-GAN（并行 GAN）**

HiFi-GAN 的突破：**用 GAN 一次性并行生成整段波形**，不再是逐点自回归。

---

## 4. HiFi-GAN 深拆

### 4.1 生成器：从梅尔谱直接"上采样"出波形

生成器是一串**转置卷积（上采样）**，把低帧率的梅尔谱逐步放大到高采样率波形：

$$
\text{梅尔谱} (80, T) \xrightarrow{\text{上采样}} (1, T \times \text{hop})
$$

配合 **MRF（Multi-Receptive Field Fusion）模块**——多个不同膨胀率的残差块并行，融合不同感受野的细节，提升音质。

### 4.2 判别器：MPD + MSD

HiFi-GAN 用**两个判别器**从不同角度挑毛病：

- **MPD（Multi-Period Discriminator）**：把波形按不同周期（2,3,5,7,11）重排成二维，判别**周期性结构**——语音的谐波、音高是周期性的；
- **MSD（Multi-Scale Discriminator）**：在不同下采样尺度上判别**整体包络与细节**。

**为什么两个**：单一判别器有盲区。MPD 抓周期结构，MSD 抓多尺度波形形态，两者互补，逼生成器同时做好周期性和整体形态。

### 4.3 损失

- 对抗损失（GAN）；
- **特征匹配损失（Feature Matching）**：让生成波形的判别器中间层特征也接近真实——比只看最终判别更稳；
- **梅尔谱重建损失**：把生成波形再转回梅尔谱，与输入梅尔谱比，保证声学一致。

---

## 5. VITS：端到端的统一

### 5.1 动机

两段式（声学模型 + 声码器）要分别训练、误差累积。VITS 把整个流程**端到端**进一个模型：

$$
\text{文本} \xrightarrow{\text{编码器}} \text{隐变量} \xrightarrow{\text{Normalizing Flow}} \xrightarrow{\text{解码器}} \text{波形}
$$

### 5.2 三大组件

1. **VAE（变分自编码器）**：把语音编码到隐变量空间，再从隐变量解码波形；
2. **Normalizing Flow**：增强隐变量分布的表达力（把简单高斯变换成复杂分布）；
3. **MAS（Monotonic Alignment Search）**：单调对齐搜索，自动学文本与语音的对齐——**不需要外部时长标签**（对比 FastSpeech 需要 teacher）。

### 5.3 VITS 的优势与代价

- **优势**：端到端、对齐自学、音质高、自然度好；
- **代价**：训练复杂、调参难、推理比 FastSpeech + HiFi-GAN 略重。

VITS 是"端到端零样本克隆"的重要基座（很多克隆系统基于 VITS 改造）。

---

## 6. 实现与验证（交付核心）

### 6.1 两条链路合成对比

```python
# 以官方预训练模型为例（具体加载方式按各仓库文档）
# 链路 A：Tacotron2(声学) + HiFi-GAN(声码)
# 链路 B：VITS 端到端

text = "大家好，这是我的自我介绍，很高兴认识你们。"

# 链路 A（伪代码骨架，实际按仓库 API）
# mel = tacotron2.synthesize(text)
# wav_a = hifigan(mel)

# 链路 B
# wav_b = vits.synthesize(text)

import soundfile as sf
# sf.write("tts_tacotron_hifigan.wav", wav_a, 22050)
# sf.write("tts_vits.wav", wav_b, 22050)
print("请分别用两个模型合成，保存为两个 wav 供盲评")
```

### 6.2 主观 MOS 盲评（交付）

MOS（Mean Opinion Score）：找 3 个朋友盲听打分（1-5 分，5 最好），不告知哪个是哪个模型。

```python
# 盲评表模板
eval_sheet = """
请为以下两条语音打分（1=差, 5=自然）：
样本 1（随机顺序）: ____ 分
样本 2（随机顺序）: ____ 分
自然度、清晰度、有无机械感，各记一句评语。
"""
```

**记录维度**：自然度、清晰度、机械感/伪影、整体偏好。这建立你的"主观评价"方法论——TTS 没有像 ASR 那样客观的 WER，主观评价是核心。

### 6.3 耗时占比分析

对两条链路，分别记录各环节耗时：

```
链路 A：文本前端 ___ms | 声学模型 ___ms | 声码器 ___ms
链路 B：VITS 端到端 ___ms
```

**观察点**：声学模型（尤其自回归）通常是链路 A 的瓶颈；VITS 端到端总耗时更短但单次前向更重。

---

## 7. 工程权衡与失效模式

### 7.1 权衡

- **自回归 vs 非自回归**：自回归质量高但慢（WaveNet/Tacotron）；非自回归快但需显式时长（FastSpeech）；
- **两段式 vs 端到端**：两段式灵活可替换、但误差累积；端到端一致性好、但训练复杂；
- **采样率**：22/24 kHz 音质好但算力高；16 kHz 省算力。

### 7.2 失效模式

1. **attention 对齐错误**（Tacotron）：跳字、重复。修复：单调约束、位置敏感 attention。
2. **时长预测不准**（FastSpeech）：语速忽快忽慢。修复：更好的时长标签、韵律控制。
3. **声码器伪影**：GAN 生成出现噪声、嗡嗡声。修复：判别器增强、训练更久。
4. **多音字/韵律平淡**：文本前端或韵律建模不足。修复：G2P 词典、显式韵律控制。

---

## 8. 延伸思考题（含解析）

**Q1**：梅尔谱 → 波形的一对多难题是什么？三种声码器各怎么破？
**A**：梅尔谱丢相位，同一谱对应无数波形。Griffin-Lim 迭代估相位（无模型、音质差）；WaveNet 自回归逐点生成（建模相位但慢）；HiFi-GAN 用 GAN 并行生成（判别器逼出自然波形，快且好）。

**Q2**：HiFi-GAN 为什么要 MPD + MSD 两个判别器？
**A**：MPD 按周期重排判别周期性结构（谐波、音高），MSD 在多尺度判别整体波形形态。单一判别器有盲区，两者互补逼生成器同时做好周期性与形态。

**Q3**：FastSpeech 为什么比 Tacotron 快且稳？
**A**：FastSpeech 用显式时长预测器替代 attention 对齐，能并行生成所有帧（非自回归），且对齐由时长决定不再依赖易错的 attention。速度数十倍提升、鲁棒性更好。

**Q4**：VITS 的 MAS 相比 FastSpeech 的时长标签，优势是什么？
**A**：FastSpeech 需要外部 teacher 的 attention 提取时长标签（依赖预训练、误差传递）；VITS 的 MAS 在训练中自动单调对齐，端到端自学，无需外部标签，更简洁一致。

**Q5**：为什么现代零样本克隆多基于 VITS 或 flow matching，而不是 FastSpeech+HiFi-GAN？
**A**：克隆需要注入参考音频的说话人信息并高保真重建音色。VITS/flow 的端到端隐变量/流建模更能表达说话人细节与多样性；FastSpeech 的确定性梅尔谱路线对音色细节表达受限。

---

## 本周交付清单

- [ ] 画「Tacotron2+HiFi-GAN」vs「VITS」两条链路对比图，标注各模块输入输出。
- [ ] 用官方模型合成你的自我介绍，保存两个 wav。
- [ ] 组织 3 人盲评，记录 MOS 与评语。
- [ ] 记录两条链路各环节耗时占比。
